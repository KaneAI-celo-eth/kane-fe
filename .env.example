# Public constants (attribution tag, chains, tokens) live in src/config/*.ts — not here.
# Only deployment-specific addresses go in .env:

# KaneExecutorFactory addresses (fill after deploy)
VITE_FACTORY_CELO=
VITE_FACTORY_SEPOLIA=
